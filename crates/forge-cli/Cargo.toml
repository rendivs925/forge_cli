[package]
name = "forge-cli"
description = "Forge CLI: definition, routing, and command handlers."
version.workspace = true
edition.workspace = true

[[bin]]
name = "forge"
path = "src/main.rs"

[dependencies]
forge-analysis = { path = "../forge-analysis" }
forge-config = { path = "../forge-config" }
forge-core = { path = "../forge-core" }
clap = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
